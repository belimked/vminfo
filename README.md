# 环境搭建图文教程

单文件静态页面，可直接部署到 GitHub Pages。

## 部署
1. 把本目录推送到 GitHub 仓库；
2. 仓库 Settings → Pages → Source 选择分支 + `/ (root)`；
3. 访问 `https://<用户名>.github.io/<仓库名>/`。

## 替换截图
把真实截图命名为 `images/step-01.png` ~ `images/step-04.png` 放进 `images/` 目录，
页面会自动用截图替换内置的 SVG 示意图（图片缺失时自动回退到示意图）。
